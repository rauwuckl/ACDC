
def get_person_id_and_distress(img):
    """
    Takes an image as a array (we could settle on something else as well) and return the persons name plus the distress
    level
    :param img:
    :return: (PersonID: String, distress_level: float)
    """
    return ("SampleName123", 0.5)